<section id="banner" class="banner">
  <div class="bg-color">
    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="col-md-12">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/"><img src="<%= image_path('favicon.png') %>" class="img-responsive"></a>
          </div>
          <div class="collapse navbar-collapse navbar-right" id="myNavbar">
            <ul class="nav navbar-nav pc_menu">
              <li class="user_menu">
                <a style="min-width: 160px">&nbsp;<i class="fa fa-bar-chart"></i>市场</a>
                <div class="menus">
                  <ul>
                    <%= render partial:'shared/market_list' %>
                  </ul>
                </div>
              </li>
              <% if current_user %>
                <li class="user_menu">
                  <a class="herd_user_info" style="min-width: 158px">
                    <img src="<%= image_path('favicon.png') %>">
                    <span>Wyckoff</span>
                  </a>
                  <div class="menus">
                    <ul>
                      <%= render partial:'shared/user_link' %>
                    </ul>
                  </div>
                </li>
              <% else %>
                <li><a href="<%= sign_up_path %>">注册&nbsp;<i class="fa fa-user-plus"></i></a></li>
                <li><a href="<%= sign_in_path %>">登录&nbsp;<i class="fa fa-sign-in"></i></a></li>
              <% end %>
            </ul>
            <ul class="nav navbar-nav mobile_menu">
              <% if current_user %>
                <%= render partial:'shared/user_link' %>
              <% else %>
                <li><a href="<%= sign_in_path %>"><i class="fa fa-vcard"></i>&nbsp;&nbsp;注册登录</a></li>
              <% end%>
              <%= render partial:'shared/market_list' %>
            </ul>
          </div>
        </div>
      </div>
    </nav>
  </div>
</section>